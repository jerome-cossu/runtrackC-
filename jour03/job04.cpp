#include <stdio.h>
#include <string.h>

int main() {
    char phrase[] = "Vive la plateforme";

    int longueur = strlen(phrase);

    printf("La longueur de la phrase est : %d\n", longueur);

    return 0;
}